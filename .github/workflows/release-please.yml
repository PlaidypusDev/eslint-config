name: Release Please

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "packages/**"

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: GoogleCloudPlatform/release-please-action@v3.2.6
        id: release
        with:
          release-type: node
          package-name: "@plaidypus-dev/eslint-configs"
          path: packages
          monorepo-tags: true

      - uses: ./.github/workflows/deploy-latest-packages.yml
        with:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        if: ${{ steps.release.outputs.release_created }}
